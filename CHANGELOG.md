# 更新日志（CHANGELOG）

本文件记录「竞拍之王」的每次功能变更与缺陷修复。
约定：**每次改错 / 修复 / 功能变更，都必须在本文件追加一条记录**（含日期、类型、内容、commit）。

格式：`- [类型] 内容（commit `xxxxxxx`）`

---

## 2026-08-14

### ✨ 新增 / 变更
- 应用界面高级化（按提示词）：整体切换为**深色高端工具主题**（`index.css` 定义色彩令牌：近黑 `#111114` 底、暖金 `#c9a962` 唯一强调色、低饱和绿/红盈亏色）；标题 DM Sans / 正文 Inter / 数据 JetBrains Mono / 中文 Noto Sans SC 字体；导航改为顶部 56px 文字链（当前页底部金色细线，移除侧边栏）；卡片去阴影改 1px 细边框 + 24px 内边距 + 大写小标题；输入框无边框、focus 底部 2px 金色细线；按钮 36px 金色主按钮/细边框次按钮；ECharts 统一深色 tooltip；图鉴管理默认表格视图（行高 48 交替底色、格数 pill、价格右对齐等宽）；历史复盘改为**时间线布局**（左侧竖线 + 金色节点，保留展开明细/修改/删除/竞拍标记/核验标记）。
- 回退电影式落地页改动（`/` 恢复为应用入口，原 `/app` 路由撤销，后端 SPA fallback 一并回退）。

### ✨ 新增 / 变更
- 历史对局支持**红品增删改查**：新增 `PUT /api/games/{game_no}/items`（整单红品列表更新，保存后自动重算 件数/格数/均格/格数组合/红品价值，并重新核验收益）；历史复盘展开区的红品明细增加「✎ 编辑红品」→ 逐行编辑 名称/格数/价格、删除行、添加红品、保存。
- 历史复盘新增**估值准确率图**：后端 `GET /api/games/accuracy` 每局用「平均格数 + 随机一件红品（以局号作种子保证可复现）」走估值引擎得预测值，预测/实际 = 准确率；前端柱状图按区间着色（绿=90-99%、黄=80-89%/101-110%、红=其他），含 100%/90%/99% 参考线，悬停显示预测/实际/样本。
- 估值准确率图优化：图表改为**折线图**（各点按准确率区间着色）；修复颜色不生效问题（ECharts canvas 不支持 CSS 变量 `var(--success)` 等，改为具体色值 `#4a9a6a`/`#9a8a4a`/`#9a4a4a`）；后端回测改为**多线程并行**（估值引擎为 numpy 计算，多线程并行加速，93 局约 10s）。
- 估值算法加**历史校准系数**：规则引擎预测系统性偏高（实测红品期望偏高 ~2.4 倍、全场偏高 ~2.9 倍），新增 `estimator.compute_calibration` 用历史对局「实际/规则预测」的中位数计算校准系数（红品全局 + 全场按均格分桶），`estimate` 输出统一应用、`/api/games/accuracy` 回测同口径应用；校准后准确率图分布改善（红 92 → 62），估值输出从偏高 2.9 倍修正为接近实际。

### 🐛 修复
- 历史复盘页布局调整：**收益走势图移到对局列表上方**；时间线条目精简为**默认折叠**（收起只显示 局号/格数组合/件数·均格/收益 + 折叠箭头，点击展开才显示 红品价值/总价值/成交价 + 操作按钮 + 红品明细），红品明细**限高滚动**避免内容过多。
- 修复标注校准页**同图两件同名红品被合并成 1 件**：本局汇总的自动并入与勾选确认两处按名称去重逻辑改为「跨图同名合并、同图内同名分别保留」（后端 `recognize_multi` 已如此处理，前端未同步导致）。
- 修复图鉴弹窗「学习/变体」标签字体重贴（两个标签叠在同一位置）。改为同一容器并排显示。
- 修复大量「图片缺失」：manifest 记录 956 条指向不存在文件的历史孤儿；从 `data/crops.bak_pre_stabilize` 备份补回 62 个藏品 777 条，清理全部孤儿索引。
- 修复 `cleanup_orphans` 因正反斜杠路径不匹配误移全部图片的严重事故：该函数改为**只读统计**，删除一律走回收箱 `data/crops_trash/`。
- 修复删除图片「删除中…」假死：浏览器原生 `window.confirm` 改为页面内确认弹窗，并增加真实进度提示。
- 修复图片加载失败（manifest 路径混用反斜杠导致 uploaded 接口返回 0 字节）：路径统一归一化 + 接口容错 + 前端缺失占位。
- 修复红品识别接口 HTTP 500：`match_crop`/`match_crops` 直接索引 `m["grid_cells"]`，对缺失该字段的 manifest 条目（重建样本）抛 KeyError，改为安全取值。
- 修复「萧何月白青花瓷瓶」识别格数返回 0：回填 manifest 中 243 条重建样本缺失的 `grid_cells`（以图鉴数据库为准）。
- 修复对局结算图两件同名藏品被合并成一个：OCR 去重由「按名称」改为「按位置」（`_board_items` 同名+位置重叠/水平接近才去重）；`recognize_multi` 仅跨图合并同名。
- 核验全部 93 局历史对局的收益字段：统一按「**收益 = 成交价 − 总价值**」补算/修正（86 局，备份 `bidking.db.bak_profit*`）；其余 7 局缺总价值/成交价，待数据补充。
- 删除 7 局缺总价值/成交价的历史对局（局 36/39/46/51/56/57/83，无法计算收益），历史复盘同步移除。
- 再次删除 6 局数据不完整的历史对局（局 32/36/39/46/51/52：缺总价值 `full_value` 或红品价值 `red_value` 或物品格数 `red_grids`），历史复盘同步移除，现存 85 局全部字段完整。
- 删除数据异常的对局 11（总价值 686,384 却成交 9,910,604、收益 9,224,220，明显失真），现存 84 局。
- 历史对局 `game_no` 重新编号：删除对局后出现 12 处断档（11/32/36/39/44/46/47/51/52/56/57/83），按原顺序重排为连续 1..84（两阶段 UPDATE 避免主键冲突，`sqlite_sequence` 同步更新）。

### ✨ 新增 / 变更
- `game_records` 新增 `won`（本人是否竞拍成功）字段：标注校准「保存本局」可勾选；历史复盘表格「竞拍」列可逐局切换（未标记 → 本人竞拍成功 → 未成功）。
- 历史复盘新增「收益走势」折线图（x=局号，y=收益；蓝色=本人竞拍成功，灰色=未成功/未标记），收益规律仅统计本人竞拍成功的对局。
- 新增多件红色品质物品（名车引擎/轮胎/座椅/转向、高冰种/色带料/青皮料/黄沙皮原石、涂装喷漆、战术防弹衣等），系统价录入。
- 标注校准页「手动添加 / 本局汇总」下拉框显示藏品价格，便于快速定位。
- 图鉴详情弹窗：支持滚轮滑动浏览、点击整图勾选、逐张删除学习图片、图片缺失占位。
- 后端代码重构：`main.py`（1135 行 God File）拆分为 `core/`（归一化、后台任务）、`services/`（图鉴匹配、估值编排）、`routers/`（按领域路由）；统一 3 处重复的名称归一化与图鉴匹配逻辑；估值融合权重提为常量可单测。
- 估值引擎性能优化：组合采样两档策略（枚举+批量）、numpy DP、向量化蒙特卡洛、加权分位数替代 20000 次重采样，整体约 **83 倍**提速（667ms → 8ms）。
- 项目上传 GitHub 并设为公开仓库：`https://github.com/stqkb/bidking`，补充 README 项目说明。
- SQLite 访问优化：图鉴全表/别名映射/估值统计（按格数统计、件数先验、全场倍率）加内存缓存（写操作后显式失效）；`db.py` 开启 WAL + busy_timeout 支持读写并发；OCR 确认后的 ML/CNN 重训改为**顺序执行**（单一后台任务，避免双线程竞争 CPU 与连接）。estimate 稳态耗时约 0.9s → 0.15s。
- 前端导航精简：移除「棋盘 CNN / 截图识别 / 图像学习」三个页面入口（仅保留 新对局估值 / 历史复盘 / 模型面板 / 图鉴管理 / 标注校准），并移除图鉴管理页「去学习选中」按钮与对应跳转逻辑。
- OCR 模块性能优化：① 图鉴名称匹配合并 `match_by_name`/`match_catalog` 共享评分核心（`_base_score`），并新增归一化名称索引缓存（`KEY_CATALOG_INDEX`），消除重复归一化与重复逻辑；② `_full_texts` 不再写临时文件，改为内存 ndarray 直传 RapidOCR；③ 新增识别结果 LRU 缓存（路径+mtime+大小+缩放档位，命中约 0.04s）；④ `process_auction_dir` 多图并行 OCR（线程池）；⑤ 修复多图并行时裁剪样本文件互相覆盖的隐患（文件名加图名前缀）。
- 标注校准「手动添加漏检红品」：下拉文字选择改为**图片网格选择**（按格数筛选后显示藏品缩略图+名称+格数+价格，点击即选中），并新增排序控件（名称 A-Z / 价值升序 / 价值降序）。
- ML/CNN 模型与视觉识别性能优化：① ResNet50 特征缓存改为**增量更新**（`crops_feats.meta` 记录源文件签名，manifest 增删少量条目时只编码新增/变更图，命中旧特征直接复用，无变化不重写盘）；指纹含文件 size+mtime 防同名替换；② ResNet50 增加 **TorchScript trace 缓存**（英文 temp 路径，中文路径下 jit.save 会失败；免每次启动构造 torchvision 模型）；③ `match_crop`/`match_crops` 的 votes 统计改为单次遍历（去 O(n·k) 嵌套）；④ `_encode` 去重复 RGB 转换；⑤ ML `build_dataset` 与 LOOCV/chrono/校准评估按数据指纹缓存（game_records+catalog_items 未变时复用，避免每次重训全量重算评估）；⑥ CNN 合成数据（40000 样本）指纹磁盘缓存，生成 2.15s → 0.045s。
- 修复深色主题下「藏品名称」文字颜色过淡：图鉴管理卡片/列表名称、标注校准图片库名称、历史复盘藏品明细、新对局估值候选项等由深灰（slate-500/600/700）统一调亮为浅色（slate-100/200/300）。
- 保存历史对局增强：标注校准「本局汇总」的总价值/成交价/收益改为**可手动编辑**；保存时自动核验 `收益 = 成交价 − 总价值`（`game_records` 新增 `profit_ok` 字段），**核验不通过的局标红且不进入模型训练**（ML `build_dataset` 排除 `profit_ok=0`）；前端实时显示核验结果。
- 历史复盘增强：新增收益排序（高→低 / 低→高 / 按局号）、收益过滤（正/负/全部）、拍下过滤（全部/本人拍下/未拍下）；盈亏列显示「⚠核验不过」标记。
- 收益走势图改为**三条线**（双 y 轴）：橙色=历史对局总价值、蓝色=历史对局收益、绿色=本人竞拍成功的收益。
- 收益走势图调整为**三个独立小图**（总价值 / 全部收益 / 本人竞拍成功收益，各自单轴），避免多线同轴互相干扰。
- 「本人竞拍成功的收益」图按**拍下次序 1..N 连续排列**（原局号不连续导致折线断开），悬停显示原局号。
- 盈亏公式统一更正为「**盈亏 = 总价值 − 成交价**」：`_check_profit_ok` 与前端核验提示同步调整；历史 87 局 `profit` 已按新公式重算（备份 `bidking.db.bak_profit3_*`），全部一致。
- 历史复盘增加**对局修正**：每局盈亏列新增「✎ 修改」按钮（核验不过的局红显），点击展开编辑 总价值/成交价/收益，保存后自动重新核验（`PATCH /api/games/{game_no}` 扩展支持价格字段）；核验不通过的对局**不参与模型训练**。
- 历史复盘增加**对局删除**：每局新增 🗑 删除按钮（点击后需再点「确认删除？」防误删），`DELETE /api/games/{game_no}` 删除后剩余对局自动重排为连续 1..N。
- 修正局 87 总价值为 750,573（原 63,660 明显低于红品价值 645,839），盈亏同步重算为 -449,427；仍存在 12 局「总价值 < 红品价值」待用户提供正确数据。
- 删除全部 12 局「总价值 < 红品价值」的异常对局（原局号 44/53/64/65/66/74/75/76/77/78/81/82），剩余对局重新连续编号（备份 `bidking.db.bak_del_bad12_*`）。
- 收益自动计算：标注校准「本局汇总」与历史复盘修改表单，输入**总价值 / 成交价**后收益**自动按「总价值 − 成交价」算出**（收益仍可手动覆盖）。

---

## 历史版本说明
早期开发记录见 git 提交历史（`git log`）。自 2026-08-14 起，所有变更在此文件留档。
