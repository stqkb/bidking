# 更新日志（CHANGELOG）

本文件记录「竞拍之王」的每次功能变更与缺陷修复。
约定：**每次改错 / 修复 / 功能变更，都必须在本文件追加一条记录**（含日期、类型、内容、commit）。

格式：`- [类型] 内容（commit `xxxxxxx`）`

---

## 2026-08-14

### 🐛 修复
- 修复图鉴弹窗「学习/变体」标签字体重贴（两个标签叠在同一位置）。改为同一容器并排显示。
- 修复大量「图片缺失」：manifest 记录 956 条指向不存在文件的历史孤儿；从 `data/crops.bak_pre_stabilize` 备份补回 62 个藏品 777 条，清理全部孤儿索引。
- 修复 `cleanup_orphans` 因正反斜杠路径不匹配误移全部图片的严重事故：该函数改为**只读统计**，删除一律走回收箱 `data/crops_trash/`。
- 修复删除图片「删除中…」假死：浏览器原生 `window.confirm` 改为页面内确认弹窗，并增加真实进度提示。
- 修复图片加载失败（manifest 路径混用反斜杠导致 uploaded 接口返回 0 字节）：路径统一归一化 + 接口容错 + 前端缺失占位。
- 修复红品识别接口 HTTP 500：`match_crop`/`match_crops` 直接索引 `m["grid_cells"]`，对缺失该字段的 manifest 条目（重建样本）抛 KeyError，改为安全取值。
- 修复「萧何月白青花瓷瓶」识别格数返回 0：回填 manifest 中 243 条重建样本缺失的 `grid_cells`（以图鉴数据库为准）。
- 修复对局结算图两件同名藏品被合并成一个：OCR 去重由「按名称」改为「按位置」（`_board_items` 同名+位置重叠/水平接近才去重）；`recognize_multi` 仅跨图合并同名。
- 核验全部 93 局历史对局的收益字段：统一按「**收益 = 成交价 − 总价值**」补算/修正（86 局，备份 `bidking.db.bak_profit*`）；其余 7 局缺总价值/成交价，待数据补充。
- 删除 7 局缺总价值/成交价的历史对局（局 36/39/46/51/56/57/83，无法计算收益），历史复盘同步移除。
- 再次删除 6 局数据不完整的历史对局（局 32/36/39/46/51/52：缺总价值 `full_value` 或红品价值 `red_value` 或物品格数 `red_grids`），历史复盘同步移除，现存 85 局全部字段完整。
- 删除数据异常的对局 11（总价值 686,384 却成交 9,910,604、收益 9,224,220，明显失真），现存 84 局。

### ✨ 新增 / 变更
- `game_records` 新增 `won`（本人是否竞拍成功）字段：标注校准「保存本局」可勾选；历史复盘表格「竞拍」列可逐局切换（未标记 → 本人竞拍成功 → 未成功）。
- 历史复盘新增「收益走势」折线图（x=局号，y=收益；蓝色=本人竞拍成功，灰色=未成功/未标记），收益规律仅统计本人竞拍成功的对局。
- 新增多件红色品质物品（名车引擎/轮胎/座椅/转向、高冰种/色带料/青皮料/黄沙皮原石、涂装喷漆、战术防弹衣等），系统价录入。
- 标注校准页「手动添加 / 本局汇总」下拉框显示藏品价格，便于快速定位。
- 图鉴详情弹窗：支持滚轮滑动浏览、点击整图勾选、逐张删除学习图片、图片缺失占位。
- 后端代码重构：`main.py`（1135 行 God File）拆分为 `core/`（归一化、后台任务）、`services/`（图鉴匹配、估值编排）、`routers/`（按领域路由）；统一 3 处重复的名称归一化与图鉴匹配逻辑；估值融合权重提为常量可单测。
- 估值引擎性能优化：组合采样两档策略（枚举+批量）、numpy DP、向量化蒙特卡洛、加权分位数替代 20000 次重采样，整体约 **83 倍**提速（667ms → 8ms）。
- 项目上传 GitHub 并设为公开仓库：`https://github.com/stqkb/bidking`，补充 README 项目说明。
- SQLite 访问优化：图鉴全表/别名映射/估值统计（按格数统计、件数先验、全场倍率）加内存缓存（写操作后显式失效）；`db.py` 开启 WAL + busy_timeout 支持读写并发；OCR 确认后的 ML/CNN 重训改为**顺序执行**（单一后台任务，避免双线程竞争 CPU 与连接）。estimate 稳态耗时约 0.9s → 0.15s。
- 前端导航精简：移除「棋盘 CNN / 截图识别 / 图像学习」三个页面入口（仅保留 新对局估值 / 历史复盘 / 模型面板 / 图鉴管理 / 标注校准），并移除图鉴管理页「去学习选中」按钮与对应跳转逻辑。

---

## 历史版本说明
早期开发记录见 git 提交历史（`git log`）。自 2026-08-14 起，所有变更在此文件留档。
