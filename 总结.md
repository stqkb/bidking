# 《竞拍之王》红品估值与出价助手 —— 项目总结

> 整理日期：2026-08-13
> 项目根目录：`E:\桌面\拍卖预测\竞拍之王项目`
> 服务地址：http://127.0.0.1:8000（双击根目录 `启动.bat` 启动）

---

## 1. 项目是什么

面向游戏《竞拍之王》的本地网页工具，核心解决三件事：

1. **红品识别**：从截图（红底棋盘、深红底、金底单件）自动识别出红色藏品，给出名称、格数、价值、置信度。
2. **对局估值**：输入红品平均格数 + 已知单件红品信息，反推格数组合，估算红品与全场总价值区间，给出推荐出价与风险等级。
3. **持续学习**：每场结算后录入真值，表格模型与视觉特征库自动重训，越用越准。

---

## 2. 技术栈与架构

```
竞拍之王项目/
├── backend/        # Python 后端
│   ├── run.py      # 启动脚本（uvicorn，启动时预热视觉模型）
│   └── app/
│       ├── main.py      # FastAPI 路由 + 红品检测（auto_detect）
│       ├── engine.py    # 规则引擎：均格反推 / 格数组合 DP / 价值聚合 / 出价
│       ├── ml.py        # 残差回归集成（贝叶斯岭 + 高斯过程 + 梯度提升）
│       ├── vision.py    # 图像学习 / 特征库 / 匹配（ResNet50 FP16）
│       ├── ocr.py       # RapidOCR 名称 / 价格 / 结算识别
│       ├── cnn.py       # 棋盘布局 CNN（合成预训练 + 真实校准）
│       ├── importers.py # Excel 图鉴 / md 对局数据导入
│       └── db.py        # SQLite 访问
├── frontend/       # React 18 + TypeScript + Tailwind + ECharts（已构建到 dist）
├── data/           # bidking.db、crops 学习图片、crops_manifest.json、特征缓存、模型文件
├── 对局数据/        # 红色图鉴 Excel（原价版）+ 各局 md（只读源数据）
├── 截图输入/        # 训练截图 / 测试集
├── 拍卖结果图/      # 结算截图
├── docs/           # README、使用说明、交接文档、本总结
└── 启动.bat
```

**关键依赖**：Python 3.11 + FastAPI + scikit-learn + PyTorch（GPU, FP16）+ onnxruntime-gpu + RapidOCR；前端已构建为静态产物，运行时无需 Node。

---

## 3. 数据现状（2026-08-13）

| 项目 | 数量 |
|---|---|
| 红色图鉴（catalog_items） | 188 件 |
| 历史对局（game_records） | 71 局 |
| 视觉图库条目（含增强变体） | 1600+ |
| 原始学习样本（非变体） | 251 张 |
| 有原始样本的藏品 | 165 / 188 |
| 无独立原始样本的藏品 | 23 件（仅有增强变体，建议补拍） |
| GPU | NVIDIA GeForce RTX 4060 Laptop |

**视觉区分度**：同款相似度中位 0.931，异款最高 0.956，区分间隙仍为负（约 -0.025）。
混淆集中：十二生肖兽首、十三幺麻将牌、钻石/项链类；这类近同图标只能靠名称 + 价格融合解决。

---

## 4. 核心算法

### 4.1 规则引擎（估值主计算）
- 输入红品均格 `c`，按 `c ≤ a/b ≤ c+0.01` 枚举（总格数 a, 件数 b），兼容 1 位小数，无解时 ±0.05 放宽；
- 动态规划枚举格数组合（规格 1/2/3/4/6/8/9/12/15/16），输出如 `1+1+1+4+4`；
- 红品价值按格数取图鉴统计（均值=期望、p10/p90=区间、中位=典型值）；
- 已知红品缩放因子校准（限 0.5–2，可关）；
- 全场总价值 ≈ 红品价值 × 实测倍数（默认 1.36，可细分）；
- 推荐出价 = 期望总价值 × margin（默认 0.84），风险按区间宽度分低/中/高。

### 4.2 表格机器学习（总价值预估）
- 以规则估值为强先验做**残差回归**，目标为 log(红品价) 与 log(全场价)；
- 集成：贝叶斯岭 + 高斯过程(RBF) + 梯度提升；特征含均格、件数、已知红品、场次序号等；
- 评估：留一交叉验证 + 按时间切分双口径；区间用残差经验分位（类 conformal）；
- 每新增结算记录自动重训。

### 4.3 视觉识别（红品识别）
- **检测**：统一红底判定（R-G≥30 且 G≈B），兼容旧版亮红 (101,66,79) 与新版深红 (≈60,30,28)，排除金底与棕褐图标；
- **单件回退**：无红底时，若截图是单件（彩色区域占图 ≥35% 且唯一主区域），整图直配图鉴，仅接受高分（≥0.80）；
- **匹配**：ResNet50（ImageNet）FP16 编码 → 与特征库余弦相似度，同名取最高分，learn 样本优先，变体投票计数；
- **速度**：多格批量一次 GPU 编码、无临时文件、启动预热；单图识别 160–250ms；
- 特征库变更（学习/删除）自动失效并重建。

### 4.4 OCR 结算识别
- RapidOCR 识别藏品名称 / 价格 / 藏品总价值 / 成交价 / 收益；
- 与红品识别并行执行，互不阻塞；
- 标注校准页支持多图上传、逐张识别、合并保存为一局。

### 4.5 棋盘 CNN（辅助）
- 16×16 棋盘布局 + 合成 4 万张预训练 + 真实对局校准；因真实样本仅 20-40，CNN 仅作辅助（v1.1），主模型为规则 + 表格 ML。

---

## 5. 近期完成的重要工作

### 识别与检测
- **修复新版深红底识别不出**：游戏红底由亮红变为深红，统一红底判定规则后新旧截图均可识别；
- **支持无红底单件识别**：金底/深底单件截图整图直配（实测百年窖藏 95.6%）；
- **样本瘦身**：南宋官窑瓶 27→12、洛神赋图 27→12、铁陨石切片 71→11、曜变天目盏 38→8（删除冗余变体，保留原始图；修复过一次误删）；
- **识别提速**：批量编码 + 去临时文件 + 启动预热，单图 160–250ms；

### 数据与模型
- 第 68 局数据修正为 2×喜马拉雅铂金鳄鱼皮包（4+4 格）并触发重训；
- 平均格数统一“截断 1 位不四舍五入”，历史数据已按此规则修正并重训。

### 前端
- 新对局估值“采样剪贴板识别”修复：识别结果与提示直接显示在采样按钮旁，异常/空结果有明确提示；
- 图鉴管理新增**表格视图**：名称 / 格数 / 图鉴价 / 交易行价 / 学习次数 / 全部对应图片（可点击放大）；
- index.html 禁用缓存（`Cache-Control: no-store`），更新后必达。

### 工程
- 项目整体归入 `竞拍之王项目` 单文件夹：前后端代码、数据、文档、启动脚本齐全；
- 重写 1609 条图库路径与 17 条数据库路径到新位置，工具脚本与文档同步更新；
- 服务启动时预热视觉模型与特征库。

---

## 6. 性能实测

| 场景 | 耗时 |
|---|---|
| 4 格截图识别（第一局） | ~160ms（稳定态） |
| 3 格棋盘图识别 | ~210ms |
| 2 格截图识别（第五局） | ~180ms |
| 单图标匹配 | ~50ms |
| 完整剪贴板流程 | 红品识别与结算 OCR 并行，总时长≈较慢者（OCR 约 1–4s） |

---

## 7. 已知问题与限制

1. **23 件藏品无独立原始样本**（仅增强变体），建议在“图像学习”补拍；
2. **相似藏品混淆**：十二生肖兽首、十三幺麻将牌、钻石/项链视觉上几乎相同，纯视觉无法区分（需名称 + 价格融合）；
3. **OCR 名称读取弱**：短名（如麻将牌“东/南/西/北”）难以通过名称匹配；
4. **无红底的多件棋盘**无法自动切分（棋盘版式不规整，需专门适配）；
5. **结算 OCR 全图较慢**（1–4s），可改为只识别结算区域条带；
6. **价值模型精度有限**：59 样本 LOOCV 平均误差约 27%、按时间切分约 41%，需更多干净结算数据。

---

## 8. 后续计划（按优先级）

1. **名称 + 价格融合进红品识别**：把格子附近 OCR 名称/价格与视觉分数融合，解决十二生肖/麻将牌类混淆（最大单项提升）；
2. **补拍 23 件缺原图藏品的样本**；
3. **结算 OCR 区域化**：只对底部结算条带做 OCR，提速数倍；
4. **无红底棋盘版式适配**：按格子几何切分 + 逐格匹配（需用户提供不同版式样本）；
5. **低置信度提示**：低于阈值不硬报第一名，提示人工确认；
6. **更强骨干网络**（CLIP / ConvNeXt 替换 ResNet50），整体提升区分度。

---

## 9. 使用与运维

```powershell
# 启动（自动打开浏览器 http://127.0.0.1:8000）
双击 竞拍之王项目\启动.bat

# 后端测试
python -m pytest backend/tests -q

# 重新构建前端（需 Node/pnpm）
cd frontend && pnpm install && pnpm build
```

**数据备份建议**：`data/bidking.db`、`data/crops_manifest.json`、`data/crops/`、`data/models/` 是核心数据，迁移/清理前先备份。
