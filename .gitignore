# 依赖与构建产物（可重新安装/构建）
node_modules/
frontend/node_modules/
frontend/dist/
dist/
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# 运行时/日志
*.log
.DS_Store
Thumbs.db
desktop.ini

# 回收箱（已删除的学习图片，不随仓库传播）
data/crops_trash/

# 本地备份（如需要可手动单独上传）
*.bak
data/crops_manifest.json.slash_bak
data/crops_manifest.bak_*.json
data/crops_manifest.json.bak
data/bidking.db.bak_*

# 临时脚本
_*.py

# 可重建的合成数据/特征缓存（体积大，不随仓库传播）
data/models/cnn_synth.npz
data/models/cnn_synth.fp

# 截图识别的临时裁剪样本（可随时重新生成，不随仓库传播）
data/auction_crops/

# 视觉识别特征缓存（26MB，可随时重建）
data/crops_feats.npy
data/crops_feats.meta

# Vite 构建临时文件
frontend/*.timestamp-*.mjs

# 内部工作记忆与剪贴板截图（不随仓库传播）
.workbuddy/
data/captures/

# SQLite 运行时临时文件
data/*.db-shm
data/*.db-wal

# 测试用截图输入（不入库）
截图输入/
